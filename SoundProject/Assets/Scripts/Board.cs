using UnityEngine;

//해당 스크립트 달고 있는 게 board 라는 것을 식별하기 위한 스크립트
//따로 작성하지는 않음

public class Board : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
